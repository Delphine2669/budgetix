{
  "name": "budgetix",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "cd ./back && node migrate.js",
    "dev": "cross-env-shell 'concurrently -c green,yellow -t \"HH:mm:ss\" -p \"{name} {time}\" \"$npm_package_config_cli:dev-*\"'",
    "dev-front": "cross-env-shell \"cd ./front && $npm_package_config_cli run dev\"",
    "dev-back": "cross-env-shell \"cd ./back && $npm_package_config_cli run dev\""
  },
  "config": {
    "cli": "pnpm"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "node": "^23.8.0",
    "sequelize": "^6.37.5",
    "toastr": "^2.1.4"
  },
  "devDependencies": {
    "cross-env-shell": "^7.0.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "node"
    ]
  }
}
